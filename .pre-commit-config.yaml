repos:
  - repo: local
    hooks:
      - id: semgrep
        name: Semgrep Security Scan
        entry: semgrep --error --config supply-chain-negotiator/.semgrep/rules.yaml
        language: system
        types: [python]
